<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:background="@color/surface"
    android:padding="24dp">

    <!-- Glowing Circle Badge with Settings/Wireless Icon -->
    <FrameLayout
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:background="@drawable/bg_status_badge"
        android:backgroundTint="@color/surface_elevated"
        android:layout_marginBottom="16dp">

        <ImageView
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_gravity="center"
            android:src="@drawable/ic_nav_settings"
            android:contentDescription="Setup Required"
            app:tint="@color/accent" />
    </FrameLayout>

    <TextView
        android:id="@+id/tvSetupRequiredTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/setup_required_title"
        android:textColor="@color/text_primary"
        android:textSize="16sp"
        android:fontFamily="sans-serif-medium"
        android:gravity="center"
        android:layout_marginBottom="8dp" />

    <TextView
        android:id="@+id/tvSetupRequiredDesc"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/setup_required_desc"
        android:textColor="@color/text_secondary"
        android:textSize="13sp"
        android:gravity="center"
        android:lineSpacingMultiplier="1.2"
        android:layout_marginBottom="24dp" />

    <!-- Action Buttons -->
    <Button
        android:id="@+id/btnOpenSetupGuide"
        style="@style/Widget.Material3.Button"
        android:layout_width="match_parent"
        android:layout_height="44dp"
        android:text="@string/setup_required_btn_guide"
        android:textColor="#090B0E"
        android:textSize="13sp"
        app:backgroundTint="@color/accent"
        android:layout_marginBottom="10dp" />

    <Button
        android:id="@+id/btnLaunchAppNormally"
        style="@style/Widget.Material3.Button.TextButton"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:text="@string/setup_required_btn_launch_external"
        android:textColor="@color/text_muted"
        android:textSize="12sp" />
</LinearLayout>
